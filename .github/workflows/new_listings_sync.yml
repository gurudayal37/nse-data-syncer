name: Daily New Listings Sync

on:
  schedule:
    - cron: '35 12 * * 1-5'  # Runs at 6:05 PM IST (12:35 PM UTC) Mon-Fri, 2h after SME sync - a 5min gap wasn't enough buffer against GitHub's cron queue drift (observed both jobs starting 72s apart despite the 5min schedule gap, causing a Dhan TOTP collision)
  workflow_dispatch:  # Allows manual trigger

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  sync-new-listings:
    runs-on: ubuntu-latest
    environment: dhan-sync

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Discover & Backfill New Mainboard Listings (Dhan)
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          DHAN_CLIENT_ID: ${{ secrets.DHAN_CLIENT_ID }}
          DHAN_PIN: ${{ secrets.DHAN_PIN }}
          DHAN_TOTP_SECRET: ${{ secrets.DHAN_TOTP_SECRET }}
        run: |
          python scripts/sync_new_listings.py

      - name: Log completion
        run: echo "New listings sync completed at $(date)"
